#ifndef FRC2014_COMMONFUNCTIONS
#define FRC2014_COMMONFUNCTIONS

namespace FRC
{
	// clamps value to between 2 limits
	float clamp(float low, float high, float value);
	float threshold(float val, float thresh);
}

#endif
